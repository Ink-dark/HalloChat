{
  "name": "hallo-chat-client",
  "version": "1.0.0",
  "main": "main.js",
  "private": true,
  "dependencies": {
    "antd": "^5.26.1",
    "electron-builder": "^24.6.4",
    "electron-log": "^5.4.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "socket.io-client": "^4.7.2",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "concurrently -k -n \"REACT,ELECTRON\" \"react-app-rewired start\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-start": "electron .",
    "build": "react-app-rewired build",
    "package": "electron-builder build --windows",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "electron": "^36.5.0",
    "wait-on": "^8.0.3"
  }
}
